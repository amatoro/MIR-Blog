    <div class="col-xs-12 ">
      <%= link_to 'Back', posts_path, class: 'btn btn-default pull-right' %>
    </div>

    <div class="col-xs-12">
      <h2><%= @post.title %></h2>
      <h6>Author: <%= @post.author %>, <%= @post.updated_at.strftime("on %m/%d/%Y") %></h6>
      <p><%= @post.content %></p>
    </div>